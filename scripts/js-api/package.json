{
  "license": "MIT",
  "name": "holo-back",
  "author": "holocycle",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "run-s build:generate build:compile",
    "build:generate": "go run main.go",
    "build:compile": "tsc -d --outDir dist src/index.ts"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^3.8.3"
  },
  "version": "0.2.4"
}
